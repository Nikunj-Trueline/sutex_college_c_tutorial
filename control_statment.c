/*

Loop:

    Loop is process which gets repeted again and again when the condition does not setisfy.

    There are two types of loop existing in real world.
    1. controlled loop
    2. infinit loop

    There are two types of loop in programming.

    1. Entery controlled loop
        - while loop
        - for loop

    2. Exit controlled loop
        - do..while loop


    - There are three important thing is like.
     1. initilization  (start)
     2. condition  (ending)
     3. updation   (inc/dec)



*/

#include <stdio.h>

int main()
{

    /*
      do{

      }while(condition);
    */

    //  hello world print 10 times.

    //  Exit controlled loop

    // int start = 100;

    // do
    // {
    //     // printf("Hello world.\n");
    //     printf("%d\n", start);
    //     start++;

    // } while (start <= 10);

    // while loop
    // entery controlled loop

    /*

    while(condition)
    {
     // block of code
    }

    */

    // int num1 = 100;

    // //  100 91

    // while (num1 > 90)
    // {
    //     printf("%d\n", num1);
    //     num1--;
    // }

    /*
    starting point - ending point

    for loop
    */

    // int table = 10;

    // for (int i = 1; i <= 10; i++)
    // {
    //     printf("%d X %d = %d\n", table, i, table * i);
    // }
}